import { TimentoApp } from "../_components/app";

export const metadata = {
  title: { absolute: "Legacy performance file" },
  description: "The original Timento performance file.",
};

export default function LegacyPage() {
  return <TimentoApp />;
}